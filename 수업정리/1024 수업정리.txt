시험문제는 프로그램 3개
블럭 채워넣기 2개
1개는 작성
복소수 과제 시험에 나옴

array 클래스
vector는 생성과 소멸에 많은 시간이 소요됨
성능 때문에 기존의 배열을 사용하는 경우가 많음
이를 해결하기 위해 array클래스를 사용

7장은 생략

246-2 중간고사에 나옴
246-5 기말고사 시험에 나옴

포인터
포인터 변수들의 메모리 크기는 8byte로 동일함
포인터 변수는 주소값을 가지기 때문에 64bit 즉 8 byte이다.

nullptr
포인터가 초기화 되지 않았다면 포인터는 임의의 주소를 가리키게 된다.
포인터가 초기화 되지 않았을 때는 nullptr로 설정

동적 할당 메모리
히프를 통해서 메모리를 할당받는다.
new 연산자를 이용하여 동적 메모리를 할당한다
new 뒤에는 자료형을 적고 하나 이상의 요소가 필요하다면[]를 붙여 안에 숫자를 적는다.